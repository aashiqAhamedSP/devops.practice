apiVersion: apps/v1
kind: ReplicaSet
metadata:
    name: my-first-replicaset
    labels:
        env: prac
spec:
    selector:
        matchLabels:
            env: prod
    replicas: 3
    template:
        metadata:
          name: nginx-pod
          labels:
              env: prod
        spec:
          containers:
              - name: nginx
                image: nginx